import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cassiotristao.sophiebalasco',
  appName: 'As Aventuras da sophie',
  webDir: 'dist'
};

export default config;
